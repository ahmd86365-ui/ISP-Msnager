services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-isp_manager}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-isp_manager}
      POSTGRES_DB: ${POSTGRES_DB:-isp_manager}
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-isp_manager}"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  postgres_data:
